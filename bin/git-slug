#!/usr/bin/env bash
if ! URL="$(git config --get remote.origin.url)"; then
    exit 1
fi

if [[ "$URL" =~ [:/]([^/:]+/[^/]+)\.git$ ]]; then
    echo "${BASH_REMATCH[1]}"
elif [[ "$1" == '--fuzzy' ]]; then
    echo "???/$(basename "$(pwd)")"
else
    exit 1
fi
